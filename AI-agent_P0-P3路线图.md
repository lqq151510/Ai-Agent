# AI Agent P0-P3 路线图

> 这是一个面向当前仓库状态的下一阶段路线图，不是现状说明。当前 MVP 已经可运行、可 smoke、可构建，下面重点解决的是稳定性、可重复交付和产品化体验。

## 当前基线

- `backend`、`web`、`cli`、`desktop` 都已经能独立构建。
- `./scripts/smoke.sh dev` 已经可以跑通真实链路。
- `/api/v1` 路径已经对齐到代码、文档和部署健康检查。
- 现在更值得做的，不是继续“加功能”，而是把交付稳定性和长期维护成本压下来。

## P0: 稳定交付

### 目标

把“能跑”变成“每次都能稳定跑”，让本地、CI、部署三条链路都尽量可重复。

### 重点工作

- 固化 smoke 默认入口，优先使用本地 OpenAI-compatible mock，不依赖外部模型可用性。
- 把 API 路径、healthcheck、README、脚本做成一致性检查，避免再次出现 `/api` 和 `/api/v1` 漂移。
- 继续清理运行产物和本机噪音，保持 `git status` 可读。
- 把 `StartupValidationRunner` 这类启动校验拆清楚，区分“硬依赖”与“可选依赖”。

### 验收标准

- `./scripts/smoke.sh dev` 可重复通过。
- `mvn -q test`、`mvn -q -DskipTests compile`、`web build/test`、`desktop build` 都不回退。
- `docker compose config` 通过，部署脚本不会被本机端口冲突轻易打断。

### 本周建议

- 先补一层“路径/配置一致性检查”。
- 继续把 smoke 默认改成 mock 优先。
- 把 README 和运维脚本的例子统一成同一套真实路径。

## P1: 模型与运行环境解耦

### 目标

让模型提供方、启动校验、业务请求之间不要互相绑死，方便换模型、换环境、换部署方式。

### 重点工作

- 把模型探活、业务对话、发布报告这三条链路解耦。
- 统一 OpenAI / LM Studio / 本地 mock 的配置入口和错误语义。
- 给 readiness 增加更清晰的降级策略，避免“业务可用但模型探活失败”把整个系统判死。
- 继续收敛 `env/dev.env`、`env/*.example`、Docker Compose 和 README 的默认值。

### 验收标准

- 任意一种模型提供方切换后，启动行为和错误提示都能预测。
- readiness 结果能清楚地区分数据库、Redis、模型三个维度。
- smoke 可以在“无真实模型”与“真实模型”两种场景下都稳定通过。

### 下周建议

- 做一次模型配置的统一整理。
- 给 readiness 和 smoke 补一组最小测试。
- 把“失败信息”改成更容易行动的提示，不要只吐原始 401/超时。

## P2: 产品体验收口

### 目标

把现在已经能用的能力，整理成真正适合日常使用的工作台，而不是一堆已经连通的接口。

### 重点工作

- 优化 Web 工作区的信息层级，减少“找入口”的成本。
- 强化会话、工具统计、发布报告的查看和导出体验。
- 让 Dev Coach 更像一个“能带着做事情”的助手，而不是只有接口。
- CLI 输出和 Web 状态保持一致，减少两端认知差。

### 验收标准

- 用户能从首页快速完成一次完整工作流。
- 工具统计、发布报告、会话导出都能找到且默认可用。
- 常见错误能给出下一步操作，而不是只报错。

### 后续建议

- 先做少而精的高频场景，不要一口气扩成全能平台。
- 优先做“重复使用率高”的体验优化。

## P3: 桌面与发布

### 目标

把桌面端和发布流程从“能打包”推进到“适合发给别人装”的状态。

### 重点工作

- 桌面端后端启动、端口冲突处理、单实例、托盘和退出流程继续收口。
- 打包脚本和发布脚本统一，减少手工步骤。
- 给桌面端准备更清晰的更新、诊断和日志入口。
- 产物和版本信息更规范，方便以后做版本追踪。

### 验收标准

- 桌面端可以在新机器上按文档顺序安装和启动。
- 打包产物、版本号、运行日志都能追踪。
- 发布流程不依赖人为记忆。

### 以后再做

- 自动更新
- 崩溃上报
- 更完整的安装包签名和分发

## 建议顺序

1. 先做 `P0`，因为它直接决定交付稳定性。
2. 再做 `P1`，把环境敏感问题压平。
3. 然后推进 `P2`，让产品更像工具而不是 demo。
4. 最后做 `P3`，把桌面和发布完善起来。

## 不建议现在做的事

- 不要先大重构前端或后端目录结构。
- 不要先引入新的复杂框架来“显得更先进”。
- 不要在模型层和桌面层同时做大改，否则验证成本会翻倍。
- 不要再新增一套和现有 `/api/v1`、smoke、compose 不兼容的路径或配置习惯。

