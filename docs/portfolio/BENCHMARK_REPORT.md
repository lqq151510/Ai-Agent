# AI Agent Knowledge Desk — 量化性能基准与 RAG 质量评测报告

> 本报告记录系统在 RAG 检索召回质量（自动化实测）、大模型语义缓存（Semantic Cache）敏感性理论推演、以及 Caffeine + Redis 多级缓存架构设计规范。所有评测数据均明确区分“真实自动化测试指标”与“离线架构设计推演模型”，严禁弄虚作假。

---

## 1. 测试环境与基线说明

- **测试硬件**：Apple MacBook Pro (Apple Silicon M5 / 32GB 统一内存 / 1TB SSD)
- **运行环境**：macOS Darwin 26.6.2，Java 21 (OpenJDK 21.0.10)，Python 3.12+
- **关键依赖版本**：Spring Boot 3.5.14, LangChain4j 0.36.2, Milvus 2.4.15, Redis 7.0, PostgreSQL 16 + pgvector
- **可复现命令**：
  - RAG 排序融合行为评测（实测）：`mvn --settings .mvn/settings.xml -pl backend test -Dtest=RAGEvaluationBenchmarkTest`
  - 离线敏感度与架构推演脚本：`python3 scripts/benchmark/rag_cache_benchmark.py`

---

## 2. RAG 检索策略对比基准 (Retrieval Benchmark - 算法测试集评测)

在包含分布式事务、Kafka 协调者、Milvus 索引调优、JWT 鉴权、多级缓存一致性等典型技术文档语料库（涵盖精确代码符号、技术参数名与自然语言概念，共 8 篇文档、13 组测试查询）的固定规则模拟评测集上，通过 `RAGEvaluationBenchmarkTest` 测试套件量化验证了 RRF（Reciprocal Rank Fusion）在基于相对排名输入时的融合计算逻辑与排序表现：

> **【口径声明】**：本基准测试采用人工构建的测试集，使用预置规则模拟各路检索源返回的关键词与语义相对排名，用于验证 RRF 算法在多源输入下的排名累加逻辑与计算正确性。**本测试数据不代表真实文本分词/FTS、真实 Embedding 模型或生产物理向量数据库的检索质量**。

| 检索策略 (Retrieval Strategy) | Hit@1 准确率 | Hit@3 召回率 | MRR 评分 (Mean Reciprocal Rank) | 平均计算耗时 (ms) | 规则模拟特性说明 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **模拟关键词排名 (Keyword / FTS 规则模拟)** | 76.9% | 84.6% | 0.8327 | ~1 ms | 基于人工构建的专有名词与精确符号匹配模拟排名 |
| **模拟密集语义排名 (Dense Vector 规则模拟)** | 84.6% | 92.3% | 0.8942 | ~4 ms | 基于人工设定的语义相关度分级与泛化匹配模拟排名 |
| **RRF 混合融合结果 (Reciprocal Rank Fusion)** | **84.6%** | **92.3%** | **0.8718** | ~9 ms | **多路排名倒数累加**：通过 \(Score = \sum \frac{1}{k + rank_i}\) (\(k=60\)) 融合两路模拟排名输出 |

### 评测结论与工程权衡（面试关键点）：
1. **指标表现与算术验证**：在该固定测试集上，RRF 融合后的 Hit@3 为 **92.3%**（模拟关键词为 84.6%，模拟语义为 92.3%），MRR 为 **0.8718**（模拟关键词为 0.8327，模拟语义为 0.8942）。此项测试直接验证了 RRF 在两路输入存在相对排名冲突时的排序融合计算逻辑。
2. **客观对待算法指标（严禁过度归因）**：未在海量真实语料与多参数对照组下充分实验前，不主观断言“RRF 改善了纯向量的语义遗漏”，亦不将 MRR 微降武断归因于平滑常数 \(k=60\)；在求职与工程交流中，如实说明为“基于固定测试集验证 RRF 排序融合的计算逻辑”。
3. **多租户元数据预过滤（Pre-filtering）**：在执行向量搜索前将 `userId` 条件直接下推到底层向量请求，实现租户级数据逻辑隔离与跨租户防穿透，杜绝内存后过滤导致的候选集污染。

---

## 3. 大模型语义缓存 (Semantic Cache) 离线敏感度推演（设计阶段原型探索）

> **【注意】**：以下数据基于蒙特卡洛随机抽样模型（`scripts/benchmark/rag_cache_benchmark.py`），用于在开发初期推演语义缓存对长尾延迟的理论改善边界，**非线上真实环境物理压测，不得作为生产实测数据写入简历**。

基于 LangChain4j 向量余弦相似度（设定判定阈值 \(\ge 0.92\)）对高频与相似问答进行语义拦截：

| 评估维度 (Dimension) | 无语义缓存基线 (Full LLM 模拟) | 启用语义缓存 (65% 目标命中率推演) | 理论推演收益说明 |
| :--- | :---: | :---: | :---: |
| **端到端平均延迟 (Avg Latency)** | ~1350 ms | ~500 ms | 命中请求无需大模型推理排队 |
| **单次命中响应 (命中场景)** | ~1350 ms | **< 30 ms** | 仅耗费本地向量计算与比对延迟 |
| **Token 消耗** | 全量计费 | 命中部分 0 Token | 避免高频重复相似问答带来的 API 消耗 |

### 工程经验沉淀（面试关键点）：
- **阈值选型原则**：阈值过高（如 0.98）导致缓存命中率极低（< 10%）；阈值过低（如 0.85）会导致“近义但逻辑相反”的问题发生误命中（例如“如何开启事务”误命中“如何关闭事务”）。工程实践表明 **0.92 ~ 0.94** 为合理平衡点。
- **瞬态与持久化边界**：语义缓存定位为高频瞬态加速层，重启可重建；主知识向量索引定位为持久资产，支持落盘快照与损坏自愈。

---

## 4. 多级缓存架构理论设计模型与一致性保障（Caffeine L1 + Redis L2）

针对知识条目与元数据读取，设计 **Caffeine 本地内存 L1 + Redis 分布式 L2 + 关系型数据库** 的多级防击穿缓存拓扑：

```
[用户请求] 
    │
    ├───> [L1: Caffeine 本地内存]  ──> 纳秒/微秒级响应 (进程内无网络开销)
    │
    ├───> [L2: Redis 分布式缓存]   ──> 毫秒级网络 I/O 响应 (跨节点共享热点)
    │
    └───> [DB: PostgreSQL/H2]     ──> 磁盘 I/O (回填 L2 与 L1)
```

### 延迟理论加权估算模型（供方案设计与容量推演）：
- 假设在成熟业务读场景下，L1 承载 80% 读流量（平均耗时 ~0.15ms），L2 承载 15% 读流量（平均耗时 ~2.1ms），DB 承载 5% 穿透流量（平均耗时 ~18.5ms）：
- 理论加权平均耗时估算为：\(0.80 \times 0.15\text{ms} + 0.15 \times 2.10\text{ms} + 0.05 \times 18.50\text{ms} = 1.36\text{ms}\)。
- 此模型展示了通过进程内缓存阻断网络 RTT 对平均延迟的显著收益，但严禁将此理论加权值（1.36ms）或静态推演吞吐冒充为生产真实压测指标。

### 防御机制与一致性保障（核心面试亮点）：
1. **防缓存穿透**：查询空对象时缓存短过期时间的特殊空标记（Null Object），结合参数校验拦截非法 ID。
2. **防缓存击穿**：对热点条目采用互斥分布式锁（Redis / 本地并发锁），仅允许一个线程回源 DB，其余线程等待复用缓存。
3. **防缓存雪崩**：基础 TTL 设置随机抖动（如 300s + \(\text{rand}(0, 60)\)s），避免整批缓存同一秒失效导致 DB 压力骤增。
4. **双写一致性**：更新数据时先更新 DB，再删除 Redis 缓存（Cache-Aside 模式）；结合本地短 TTL 兜底，保证并发读写下的最终一致性。

---

## 5. 总结与简历引用规范

在简历或面试交流中，**严守求真底线，不造假、不夸大**：
- ✅ **推荐阐述**：“在包含 8 篇技术文档、13 组测试查询的固定规则模拟评测集上，通过自动化测试验证 RRF 混合检索的融合计算逻辑；该测试集上 RRF 的 Hit@3 为 **92.3%**，验证了多源排序融合算法的相对排名表现（如实说明为规则模拟评测集，不代表真实分词与物理向量数据库检索质量）。”
- ✅ **推荐阐述**：“针对大模型高频重复问答引入高维向量语义缓存层，基于余弦相似度（阈值 ≥0.92）拦截相似请求，大幅降低 API 调用开销与响应等待。”
- ✅ **推荐阐述**：“落地 Caffeine + Redis 两级缓存拓扑，设计防击穿互斥锁、防雪崩随机抖动、防穿透空值缓存与 Cache-Aside 双写淘汰机制，保障高并发下的数据一致性与稳定性。”
- ❌ **严禁表述**：严禁在简历中写入“实测 32,000 QPS”、“实测节约 64.8% Token”等未经真实物理集群压测的虚构常量。

