# 改进建议

> 本文档持续记录项目各阶段的改进建议。每次 review 后追加，按时间倒序排列。

---

## 2026-04-30 — 首次全面审查

### 架构层面

#### 1. 工具调用机制过于简单（keyword matching）
- **位置：** `backend/.../agent/tooling/AgentToolOrchestrator.java:23`
- **现状：** 用 `message.contains("目录")` 关键词匹配决定执行什么工具
- **建议：** 改用模型的 function calling / tool use 原生能力，让模型自己决定调用哪些工具，支持多轮工具调用循环

#### 2. 工具结果注入方式不符合标准
- **位置：** `backend/.../agent/service/AgentService.java:170`
- **现状：** 工具执行结果直接拼在最后一条 user message 后面
- **建议：** 使用标准的 `role: "tool"` 消息格式，而非拼接在 user message 中

#### 3. 上下文窗口管理用消息条数而非 token 计数
- **位置：** `backend/.../agent/service/AgentService.java:163-165`
- **现状：** 固定取最后 20 条消息，不做 token 计数
- **建议：** 基于 token 计数截断，避免长消息导致上下文溢出

---

### 后端代码质量

#### 4. @SuppressWarnings("unchecked") 泛滥
- **位置：** `OpenAiModelProvider.java`、`SystemDiagnosticsService.java`
- **现状：** 大量使用原始 `Map` 类型
- **建议：** 定义专用 DTO 类或使用 TypeReference 做类型安全反序列化

#### 5. 错误处理不一致
- **位置：** `AgentService.java:70`、`AgentController.java:211`
- **现状：** `catch (Exception ignored) { // best effort }` 静默吞异常
- **建议：** 至少 log warn 级别日志

#### 6. [已解决] 注册接口无限流保护
- **位置：** `AuthController`
- **现状：** `/api/auth/register` 和 `/api/auth/refresh` 无速率限制
- **建议：** 对注册和刷新令牌端点添加限流

#### 7. [已解决] 密码无强度校验
- **位置：** `AuthService.register()`
- **现状：** 接受任意长度密码
- **建议：** 添加最小长度（8位）、复杂度要求

#### 8. 会话列表无分页
- **位置：** `SessionService.listSessions()`
- **现状：** 全量返回所有会话
- **建议：** 添加分页参数，避免数据量增长后性能问题

#### 9. 线程池未使用 Spring 管理
- **位置：** `AgentController.java:51-70`
- **现状：** 手动 `new ThreadPoolExecutor`
- **建议：** 使用 `ThreadPoolTaskExecutor` Bean，便于 lifecycle 管理和 metrics 集成

---

### 前端

#### 10. [已解决] 巨型组件 — App.tsx 670 行
- **位置：** `web/src/App.tsx`
- **现状：** 约 30 个 useState，所有业务逻辑集中在一个文件
- **建议：** 拆分为细粒度 hooks 和 Workspace 领域组件（App.tsx 现已减至 ~210 行）

#### 11. 流式渲染性能问题
- **位置：** `App.tsx` sendMessage 中
- **现状：** 每次 chunk 触发全量 messages 数组复制
- **建议：** 使用 useRef + 批量更新，长对话场景引入虚拟列表

#### 12. 无前端测试
- **现状：** `web/` 模块零测试文件
- **建议：** 至少添加核心交互的集成测试

#### 13. 无路由管理
- **现状：** 用条件渲染切换登录/工作区视图
- **建议：** 引入 react-router，支持深层链接和浏览器导航

---

### CLI

#### 14. CLI 零测试覆盖
- **现状：** `cli/` 模块无测试
- **建议：** 添加核心命令的单元测试和集成测试

#### 15. 不支持交互式 REPL
- **现状：** 只支持单次命令
- **建议：** 实现 REPL 循环，支持连续对话

---

### 运维与安全

#### 16. Docker 健康检查缺失
- **位置：** `docker-compose.yml`
- **现状：** backend 和 web 服务无 healthcheck 定义
- **建议：** 为 backend 添加 `curl /api/system/health/ready` 健康检查

#### 17. SSE 连接无优雅关闭
- **位置：** `AgentController.java`
- **现状：** `shutdown()` 后不 `awaitTermination`
- **建议：** shutdown 后等待一定时间，让进行中的流正常结束

#### 18. 缺少 API 版本化
- **现状：** 所有端点 `/api/...` 无版本前缀
- **建议：** 添加 `/api/v1/...` 版本前缀

#### 19. access token 无法主动撤销
- **现状：** 有 refresh token 轮转但 access token 无黑名单机制
- **建议：** 短有效期 access token + 黑名单，或改用 OAuth2 标准方案

---

### 优先级总览

| 优先级 | 项目 | 理由 |
|--------|------|------|
| P0 | #1 Function calling 替代关键词匹配 | 核心能力缺陷 |
| P0 | #3 Token 计数替换消息条数截断 | 上下文溢出风险 |
| P1 | ✅已解决 #6 注册限流 + #7 密码强度 | 安全风险 |
| P1 | ✅已解决 #10 App.tsx 拆分 | 持续迭代可维护性 |
| P2 | #11 流式渲染性能 | 长对话卡顿 |
| P2 | #16 Docker 健康检查 | 部署可靠性 |
| P3 | #14/#12 CLI/前端测试、#18 API 版本化 | 长期工程健康 |
